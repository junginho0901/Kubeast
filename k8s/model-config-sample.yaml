apiVersion: ai.kubeast.io/v1alpha1
kind: ModelConfig
metadata:
  name: openai-default
  namespace: kubeast
spec:
  provider: openai
  model: gpt-4o-mini
  baseURL: https://api.openai.com/v1
  apiKeySecretRef:
    name: kubeast-secrets
    key: OPENAI_API_KEY
  enabled: true
  isDefault: true
